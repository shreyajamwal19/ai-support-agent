# Optional: only needed to exercise the LLM generation/judge paths.
# The default pipeline (rule-based intent, TF-IDF retrieval, extractive generation) runs
# with no API key at all.
ANTHROPIC_API_KEY=
